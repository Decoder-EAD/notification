spring:
  cloud:
    config:
      username: ${CONFIG_SERVER_USERNAME}
      password: ${CONFIG_SERVER_PASSWORD}

ead:
  service-registry:
    url: ${EUREKA_URL}
    app-domain-name: ${APP_DOMAIN_NAME}